name: Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  site:
    name: checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Links, tags and image sizes
        run: python3 .github/scripts/check_site.py
